# PacketScope

**PacketScope** — это учебное desktop-приложение для захвата, анализа, фильтрации и сохранения сетевого трафика.  
Проект разработан на языке **Python** с использованием принципов **объектно-ориентированного программирования**.

Приложение является упрощённым аналогом сетевого анализатора трафика и может использоваться для изучения сетевых пакетов, протоколов TCP/IP и базовой диагностики сетевой активности.

---

## Возможности приложения

PacketScope позволяет:

- захватывать сетевые пакеты в реальном времени;
- выбирать сетевой интерфейс для анализа;
- использовать BPF-фильтры до начала захвата;
- отображать пакеты в таблице;
- подсвечивать разные типы трафика цветами;
- определять популярные протоколы по портам:
  - HTTP;
  - HTTPS;
  - DNS;
  - SSH;
  - FTP;
  - SMTP;
  - POP3;
  - IMAP;
  - RDP;
  - DHCP;
  - NTP;
  - MySQL;
  - PostgreSQL;
  - Redis;
- просматривать подробную структуру выбранного пакета;
- просматривать payload в режимах:
  - ASCII;
  - HEX;
  - Raw Scapy;
- сохранять захваченный трафик в форматы:
  - `.pcap`;
  - `.pcapng`;
- открывать ранее сохранённые `.pcap` и `.pcapng` файлы;
- фильтровать уже захваченные пакеты по протоколу, IP-адресу, порту или тексту;
- отображать статистику трафика;
- показывать топ IP-адресов;
- показывать DNS-домены;
- выполнять простой анализ подозрительной активности;
- экспортировать отчёты в:
  - `.csv`;
  - `.txt`.

---

## Назначение проекта

Проект создан в рамках курсовой работы по дисциплине **«Объектно-ориентированное программирование»**.

В приложении используются основные принципы ООП:

- **инкапсуляция** — логика захвата, анализа, фильтрации, сохранения и интерфейса разделена по классам;
- **абстракция** — пользователь работает с графическим интерфейсом, не взаимодействуя напрямую с низкоуровневыми сетевыми механизмами;
- **композиция** — главное окно приложения объединяет объекты анализатора, сниффера, фильтра, статистики и хранилища;
- **модульность** — каждый файл проекта отвечает за отдельную часть функциональности.

---

## Используемые технологии

- Python 3.11+
- PyQt6
- Scapy
- python-pcapng

---

## Структура проекта

```text
PacketScope/
│
├── main.py
├── requirements.txt
├── README.md
│
├── app/
│   ├── __init__.py
│   │
│   ├── core/
│   │   ├── __init__.py
│   │   ├── sniffer.py
│   │   ├── packet_analyzer.py
│   │   ├── packet_filter.py
│   │   ├── capture_storage.py
│   │   ├── traffic_statistics.py
│   │   ├── security_analyzer.py
│   │   └── report_exporter.py
│   │
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── main_window.py
│   │   ├── packet_table.py
│   │   ├── packet_details.py
│   │   └── statistics_panel.py
│   │
│   ├── models/
│   │   ├── __init__.py
│   │   └── packet_info.py
│   │
│   └── utils/
│       ├── __init__.py
│       └── network_interfaces.py
│
└── captures/
```



## Примеры BPF-фильтров

BPF-фильтр применяется до начала захвата пакетов.

Примеры фильтров:

```
tcp
udp
icmp
dns
web
http
https
ssh
rdp
ntp
dhcp
tcp port 443
tcp port 80
tcp and (port 80 or port 443)
port 53
host 8.8.8.8
```

В приложении также поддерживаются короткие алиасы:

```
http  → tcp port 80
https → tcp port 443
dns   → port 53
web   → tcp and (port 80 or port 443)
ssh   → tcp port 22
rdp   → tcp port 3389
ntp   → udp port 123
dhcp  → udp and (port 67 or port 68)
```

## Возможные проблемы
### Приложение не видит сетевые интерфейсы

**Возможные причины:**

- приложение запущено без прав администратора;
- на Windows не установлен Npcap;
- выбран неправильный сетевой интерфейс;
- отсутствуют системные библиотеки для захвата пакетов.

**Решение:**

- **Windows:** установить Npcap и запустить приложение от имени администратора;
- **macOS:** попробовать запуск через sudo;
- **Linux:** запустить приложение через sudo.

## Ограничения проекта
- приложение не расшифровывает HTTPS/TLS-трафик;
- анализ безопасности является базовым и учебным;
- для захвата сетевых пакетов могут требоваться права администратора;
- Windows-версия требует установленный Npcap;
- проект предназначен для учебного использования.

## Автор

**Новгородский государственный университет имени Ярослава Мудрого** 
**Политехнический институт** 

**Автор:** Павлов Ян, группа 4093 

Проект разработан в рамках курсовой работы по дисциплине «Объектно-ориентированное программирование».

**Название проекта:** PacketScope

## Лицензия

`Проект предназначен для учебного использования.`